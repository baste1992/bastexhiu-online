<!DOCTYPE html>
<html lang="sq">
<head>
<meta charset="UTF-8">
<title>BASTEXHIU</title>
<style>
body { font-family: Arial; background:#f9f9f9; margin:0; padding:20px; }
header { background:#00a651; color:white; padding:25px; font-size:32px; text-align:center; border-radius:8px; }
.match { background:white; margin:15px 0; padding:15px; border-radius:8px; border:1px solid #ccc; }
.odds span { background:#00a651; color:white; padding:6px 10px; border-radius:5px; margin-right:8px; display:inline-block; }
</style>
</head>

<body>

<header>BASTEXHIU</header>

<div id="matches"></div>

<script>
let matches = [
 {home:"Manchester United", away:"Liverpool", odds:{1:2.1,X:3.2,2:3.0}},
 {home:"Arsenal", away:"Chelsea", odds:{1:1.9,X:3.5,2:4.0}},
 {home:"Barcelona", away:"Real Madrid", odds:{1:2.5,X:3.0,2:2.8}}
];

function randomize(o){ return (o*(0.95+Math.random()*0.1)).toFixed(2); }

function render(){
 document.getElementById("matches").innerHTML="";
 matches.forEach(m=>{
  m.odds[1]=randomize(m.odds[1]);
  m.odds.X=randomize(m.odds.X);
  m.odds[2]=randomize(m.odds[2]);
  document.getElementById("matches").innerHTML+=`
   <div class="match">
    <b>${m.home} vs ${m.away}</b><br><br>
    <div class="odds">
     <span>1: ${m.odds[1]}</span>
     <span>X: ${m.odds.X}</span>
     <span>2: ${m.odds[2]}</span>
    </div>
   </div>`;
 });
}

render();
setInterval(render,4000);
</script>

</body>
</html>
